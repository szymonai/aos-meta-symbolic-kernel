# Application Profiles

Run concrete application cases through the public AOS runtime:

```bash
python examples/application-profiles/run_profiles.py --check
```

The cases are plain JSON inputs. The runner verifies the expected verdict and
evidence replay for each case.
